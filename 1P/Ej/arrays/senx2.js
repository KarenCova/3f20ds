let x = Math.PI/2;
let sn = 0, sn2 = []
    function f(){
        let n = 10,
            k = 1;
            for ( m =1; m < n; m++){
                k = k * m;
                return k;
            }
    } for(i = 0 ; i<10; i++){
        f()
            let s = Math.pow(-1, i) * (Math.pow(x, 2 * i + 1) /f( 2 * i + 1));
            sn += s;
            sn2[i] = sn;
            sn++;
    } console.log(sn2);